# Fundal Image Analysis

# Files and folder description

*   notebooks/ contains all notebooks of all experiments done so far
*   utils.py - contains all utility functions for processing fundal images like loading datasets, creating training patches and any preprocessing common to all tasks
*   requirements.txt dependencies. Install the dependent packages using the following command

    `pip install -r requirements.txt` 
